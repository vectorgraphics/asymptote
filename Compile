MAGIC=magic # Hide asy rule from Makefile.
include Makefile

asy: $(FILES:=.o)
	$(CXX) $(OPT) -o $(NAME) $(FILES:=.o) $(LIBS)

	; \
	sed 's,\($*\)\.o[ :]*,\1.o $@ : ,g' < $@.$$$$ > $@; \
	rm -f $@.$$$$

depend: $(FILES:=.d)

include $(FILES:=.d)
